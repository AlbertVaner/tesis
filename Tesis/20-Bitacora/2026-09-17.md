---
fecha: 2026-09-17
---
## Hecho hoy
- Latencia de la cámara Amcrest medida a ojo en ~800 ms con el stream principal. Diagnóstico y arreglo abajo.

## Agentes
- **Claude**: latencia de la cámara IP. `video_source.py` abre el decodificador con un solo hilo (`CAP_PROP_N_THREADS=1`; OpenCV abría uno por núcleo y entrega cada frame `hilos−1` frames tarde, ~230 ms). Nuevo `external/gesture_detection/configurar_camara.py` que deja el sub-stream en 704x480 @ 30 fps H.264 CBR sin audio (sólo escribe las claves que difieren; sin `--aplicar` únicamente muestra). Ejemplos y docs pasan a `subtype=1`. 7 pruebas nuevas, suite de `gesture_detection` en verde, `--help` de `control_camara_dron1.py` correcto. Pendiente medir con `mapeo3d/apps/diagnose_camera.py` antes y después.
- **Codex**:
- **Gemini**:

## Bloqueos y dudas
- La cámara puede rechazar algún valor del sub-stream (resolución o bitrate no admitidos): el script lo dice y hay que verlo en la web de la cámara.

## Mañana
- Correr `configurar_camara.py --aplicar`, medir latencia con `diagnose_camera.py --sin-pose`, y luego el vuelo por gestos con `subtype=1`.
