---
fecha: 2026-09-08
---
## Hecho hoy
- Documento de contexto del vault: `Contexto del proyecto.md`.
- Análisis de la oscilación con evidencia de 17 sesiones CSV: no es exclusiva de dos drones. Nota en `60-Analisis` y tareas T-003 y T-004.
- Análisis de duplicación en `gesture_detection`, `mapeo3d` y `two_drones`: ~1.000 líneas ahorrables sin cambiar comportamiento y ~950 más con borrados a confirmar. Nota en `60-Analisis`.
- Grafo graphify del repositorio unificado.
- Decisiones del autor aplicadas: borrados los cinco controles dedicados al Flow Deck (paneles de uno y dos drones, cámara Flow Deck dual, diagnóstico y hover) con su cascada en `tk_keys.py`; `detectar_gestos_3d.py` fusionado en `probar_vocabulario.py --solo-dinamicos`; funciones muertas de `pose_overlay.py` fuera.
- Tests a pytest: `pytest.ini` y `conftest.py` en la raíz, once runners propios convertidos, `python -m pytest -q` es la validación canónica (AGENTS.md y README actualizados). 376 pruebas en verde con el `.venv` de tesis.
- T-001 cerrada a `revisar`: el `.venv` de tesis ya cubría mapeo3d; `requirements.txt` unificado.
- Validación del reconocedor DTW: validación anidada por sujeto y métricas por clase (`recognition/evaluacion.py`, nuevo, con la matriz de confusión que estaba duplicada en dos scripts). La cifra publicable del vocabulario de cinco gestos es **77.6 % con macro-F1 0.77**, no el 79.5 % de antes: ese umbral se elegía sobre las mismas distancias que reportaba. Medido también que sumar `saludar` y `reposo` al rechazo empeora (69.5 %) porque `saludar` colisiona con `ven_aca`. Nota en `60-Analisis`, CSV en `results/data/validacion_dtw/2026-09-08/`.
- Umbral por gesto implementado (`--umbral-por-clase`): `aplaudir` necesita 0.373 contra un global de 0.708, y con su propio umbral deja de tragarse a los demás. Los comandos inventados a partir de un movimiento ajeno bajan de 17 a 10 de 41 (vocabulario de 5) y de 14 a 6 de 30 (banco de 3), a costa de que `aplaudir` se lea menos. **Activado por defecto** por decisión del autor; los dos bancos de `models/` reconstruidos con umbrales por gesto y `--umbral-unico` conserva el comportamiento anterior. 392 pruebas en verde.
- Incidente: `thesis/` apareció en la Papelera de Windows a las 09:27 (borrado desde el Explorador, no por un script). Restaurada desde git más las cuatro figuras `joystick_marker_espacio_3d*` que no estaban versionadas. Nada se perdió.

## Agentes
- **Claude**: contexto, análisis, grafo, borrados, fusión, pytest, T-001 y la validación del reconocedor DTW.
- **Codex**: pendiente T-004.
- **Gemini**: sin usar todavía.

## Bloqueos y dudas
- Comprobar en vuelo que `aplaudir` sigue siendo usable para conmutar de modo con su umbral nuevo (0.373 contra 0.708 global): pasa de leerse 9 de cada 10 veces a 3 de cada 4. Si estorba, subir sólo ese umbral, no volver al único.
- Decisiones pendientes del autor listadas al final de la nota de optimización (borrados y fusiones).
- T-003 requiere vuelo de validación en el Robotat.

## Mañana
- Grabar dos o tres sujetos más para el vocabulario: el banco descansa en 5 personas y el umbral varía un 33 % según a quién se deje fuera. Es lo que más mueve la cifra.
- Instalar Dataview y revisar el Tablero.
- Revisar T-001 y confirmar el commit de hoy (borrados, fusión, pytest).
- Confirmar por qué `thesis/` fue a la Papelera y si hay que versionar las figuras nuevas.
