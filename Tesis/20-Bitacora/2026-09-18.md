---
fecha: 2026-09-18
---
## Hecho hoy
- La cámara Amcrest pasó por tres montajes: girada 90° (imagen enderezada con `Rotate90=2`), y por la tarde **boca abajo en el techo** (`Flip` + `Mirror`), que es como queda. Latencia: sub-stream 640x480 sin audio, 33 fps.
- Primer vuelo por gestos con la cámara IP del techo siguiendo al operador (16:29 y 16:43, un dron): funcionó bastante bien; seguir y orbitar se quedaban cortos de velocidad.
- Dos grabaciones guiadas de los gestos estáticos con la cámara del techo (17:02 y 17:03), con las que se recalibraron las reglas.
- Primer vuelo de los dos drones en formación por gestos (17:33, 110 s): despegue, seguir, estáticos y órbita. Lo terminó el supervisor de separación a 0.29 m.
- Segundo vuelo en formación (17:51, 67 s): los seis estáticos funcionaron con `--rumbo 180`. El Dron 2 cayó con motores cortados desde 0.87 m por telemetría congelada.
- Tercer vuelo en formación (18:05, 147 s): el vocabulario completo con dos drones y primer vídeo de la interfaz (`results/captures/control_camara_dron1/2026-09-18/sesion_180534.mp4`).
- Vuelos de las 18:46 y 18:50: el dron siguió al operador sin señal y no hubo forma de pararlo con el aplauso (59 s). Tras los arreglos, "ya funcionó mucho mejor".

## Agentes
- **Claude**: cámara IP montada de lado. Aplicado `VideoInOptions[0].Rotate90=2` en cam3 y comprobado con snapshot y con el sub-stream RTSP (480x640, recto, sin espejo). `configurar_camara.py` gana `--rotar {0,90,270}`. `CamaraPTZ.aplicar` lee la rotación y cruza los ejes del cuadro con los motores de pan y tilt (`MOTOR_SEGUN_ROTACION`); `mover` sigue recibiendo códigos de motor. Ventanas sin deformar con `visualization/ventana.py` en los tres probadores y en `control_camara_dron1.py`, que además escala el cuadro a 720 de alto y lo completa a 960 de ancho para que quepa el panel. 12 pruebas nuevas; suite completa 729 en verde; `--help` de los cinco lanzadores correcto. **No validado con los motores**: el signo de los ejes cruzados sale de la geometría.
- **Claude**: `--rtsp env` en los seis lanzadores con cámara IP: `external/gesture_detection/camara_env.py` arma la URL con `CAM_HOST`, `CAM_USER`, `CAM_PASSWORD` del entorno o del `.env` de la raíz (ignorado por git; `.env.example` nuevo). Corregido el fallo de `seguir_persona.py` introducido hoy: el parámetro booleano `mostrar` de `bucle` tapaba la función importada (`TypeError: 'bool' object is not callable`). 7 pruebas nuevas; comprobado `configurar_camara.py --rtsp env` contra la cámara, sólo lectura.
- **Claude**: seguimiento vertical del PTZ. Nuevo `centro_encuadre` en `ptz/seguidor.py` con `--encuadre {cuerpo,pecho,torso}` en los tres lanzadores con `--seguir`: por defecto lleva el pecho al centro de la cámara y recorta el error para no cortar cabeza ni pies; una parte del cuerpo fuera de su margen dispara siempre un paso. Zona muerta vertical por defecto de 0.16 a 0.12. `Seguidor` no cambia. 12 pruebas nuevas y una actualizada. **Sin validar con la cámara.**
- **Claude**: la cámara "no avanzaba más" hacia la derecha. Causa: montada de lado, el seguimiento horizontal lo hace el tilt, que va de −4° a 79° (leído de la cámara), y estaba en su tope inferior (−6.3°). No tiene arreglo por software: hay que girar el soporte con el tilt a mitad de recorrido. `ControlPTZ` ya no empuja contra el tope y lo expone en `control.tope`; los tres programas lo muestran en rojo. `seguir_persona.py` imprime el recorrido al arrancar y gana `--ir-a PAN TILT` (`PositionABS`, sin probar con la cámara). 6 pruebas nuevas.
- **Claude**: mirar al frente antes de buscar a la persona. `CamaraPTZ.mirar_al_frente` y `--frente PAN TILT` / `--sin-frente` en los tres programas con seguimiento; el frente sale de `CAM_FRENTE_PAN`/`CAM_FRENTE_TILT` del `.env` (180, 0) y `seguir_persona.py --guardar-frente` guarda la posición actual. Medido con la cámara: `PositionABS` usa `pan = 180 − leído`, y por eso el `--ir-a 180 37` de antes la mandó hacia atrás (corregido en `ir_a`; `--ir-a` sustituido por `--frente`). Repetibilidad de volver a una posición: 5.5 de diferencia media frente a 2.9 de ruido y 49.9 desplazada. **Queda un problema físico**: la cámara reporta 180/0 pero mira hacia atrás, boca abajo y con su cable delante del lente. 9 pruebas nuevas.
- **Claude**: preparación de la prueba con tres drones sobre el núcleo Robotat. Nuevo `controllers/two_drones/control_tres_drones_robotat.py` (hereda de `PanelDosRobotat`, ahora parametrizado por las claves de `drones`; Dron 3 con I/J/K/L, Y/H y U/O). `SupervisorSeparacion` y `separacion()` generalizados a N drones (todos los pares, sólo pares en vuelo, un dron sin pose no apaga la vigilancia). `DronSimulado` arranca al Dron N a 0.9·(N−1) m en Y. El Dron 3 no tiene identidad por defecto: sin `--dry-run` exige `--uri3` y `--topic3`; con dos Crazyradio comparte antena y el programa lo avisa. 11 pruebas nuevas; prueba de humo del panel con tres simulados (despegue, `i` mueve sólo al Dron 3, aterrizaje). **Sin validar con hardware**: falta la identidad del Dron 3 y medir la cadencia de `extpos` con la antena compartida.
- **Claude**: montaje boca abajo. El tope hacia la derecha es mecánico (tilt −4° a 79°, sin límite por software en la cámara), así que el operador va a montar la cámara boca abajo para que el horizontal vuelva a ser el pan. Medido en la cámara: `Flip` voltea el sensor en vertical y `Mirror` en horizontal, antes que `Rotate90`; 180° = `Flip` + `Mirror`. `configurar_camara.py --rotar 180` y `CamaraPTZ.codigo_motor` deshace rotación y volteos. 4 pruebas nuevas. Sin validar con la cámara ya montada.
- **Claude**: camara ya montada boca abajo. Seguia con `Rotate90=2` (la persona salia acostada): aplicado `--rotar 180`, imagen derecha y apaisada comprobada con captura. **Sentido de los motores validado por primera vez**, por correlacion de fase entre capturas: con `Flip`+`Mirror` el firmware ya invierte los motores (pulso `Left` = vista a la izquierda, `Up` = vista arriba, y `Up` baja la lectura del tilt de 2.0 a -6.3). La inversion que yo habia deducido por geometria hacia girar la camara al reves en los dos ejes: quitada de `codigo_motor`; `sentido_tilt` nuevo para el tope. Encoder: la camara rechaza 704x480 en el sub-stream (HTTP 400) y con un valor invalido ignoraba en silencio todo el lote de `Encode`; ahora 640x480 y una clave por peticion. Aplicado: audio fuera en los dos streams, sub-stream CBR 1024. La tabla de `Rotate90` (camara de lado) sigue sin validar y, visto esto, puede que el firmware tambien la compense.
- **Claude**: boca abajo no seguia hacia la derecha. Medido con pulsos y correlacion de fase: con la lectura del pan en 180 el motor no gira nada a la derecha (0 px, ni a velocidad 3) y a la izquierda si. La lectura 180 es el **tope mecanico del pan** (`fisico = 180 - leido`, recorrido 1-354), y era justo el frente guardado. `pan_fisico`, `limites_pan` y `sentido_pan` nuevos; `ControlPTZ` detecta tambien el tope del pan y `seguir_persona.py` avisa al arrancar. Arreglo fisico pendiente: llevar el pan al centro (`--frente 3 6`), girar la base media vuelta y guardar el frente. 9 pruebas nuevas.
- **Claude**: la base no se puede girar. `ControlPTZ` da la vuelta por el otro lado cuando el pan choca con su tope dos ordenes seguidas (`ir_a` al otro extremo, 30 grados pasado el sector muerto). Validado con la camara: 5.2 s, queda a 36 grados a la derecha y desde ahi sigue. **Riesgo: ~5 s sin gestos, tampoco el de paro**; avisado en pantalla y en los README. Quitado el ir al frente por defecto: `--frente` es ahora optativo y `--sin-frente` desaparece. 6 pruebas nuevas.
- **Claude**: seguir y orbitar mas rapidos, a peticion tras el vuelo. Los dos saturaban en el tope fijo de 0.30 m/s de `vuelo_camara.py` (la orbita nunca iba a su `velocidad_orbita`: el termino proporcional ya vale 0.39 m/s). Nuevos `--velocidad-tope` (0.30 por defecto, techo 0.60) y `--velocidad-orbita` en `control_camara_dron1.py`; los valores por defecto NO cambian. 2 pruebas nuevas. Sin validar en vuelo.
- **Claude**: diagnostico de los gestos estaticos con la camara IP en el techo (sin cambios de codigo). Vuelos de hoy: en modo estatico el 92-98 % de los frames es NO_GESTURE; solo ARRIBA llega a confirmarse; ABAJO y ADELANTE parpadean 1-5 frames (confianza 0.1-0.3) y DERECHA, IZQUIERDA y ATRAS no aparecen. Calidad de landmarks alta (0.98): no es visibilidad. Medido en vivo sobre una persona de pie con los brazos colgando: las reglas ven los brazos a 31 y 41 grados de la vertical (nominal 9), con componente Z de +0.5: el reposo cae DENTRO del cono de ABAJO (23-67 grados) y roza el limite de 45 del brazo parado, que anula los gestos de un brazo. Igual en sub-stream (persona ~180 px) que en principal (~250 px): no es la resolucion. Falta una grabacion guiada del operador con `probar_gestos_3d.py --rtsp env --practica` para corregir con datos.
- **Claude**: gestos estaticos recalibrados con dos grabaciones guiadas del operador (620 frames). Causas: `EXTENSION_MIN` 0.85 rechazaba los brazos laterales (se miden a 0.75-0.84 desde el techo), cono de 22 estrecho y reposo rozando su cono. Cambios en `body_3d_rules.py`: extension 0.70, cono 30 para ARRIBA/ADELANTE/IZQUIERDA/DERECHA (ABAJO y ATRAS en 22), reposo 27. Re-simulado: DERECHA 0->94 %, IZQUIERDA 0->66 %, sin gestos equivocados. Descartada con datos una correccion de sesgo en Z (rompia ARRIBA). 5 pruebas nuevas con las posturas medidas y una invariante reescrita. Analisis en `60-Analisis/2026-09-18 Gestos estaticos con la camara IP en el techo.md`. Sin validar en vivo.
- **Claude**: dos drones con el vocabulario de cuerpo. `control_camara_dron1.py --dron ambos` vuela los dos en formacion sobre el nucleo Robotat; la coordinacion esta en `controllers/two_drones/formacion_camara.py` (`VueloFormacion`, `SeguidorFormacion`), que no importa nada de `single_drone/`. Seguir: anclas a +-50 grados de la direccion media (0.69 m entre si). Orbitar: en oposicion, regulando la velocidad con el desfase (`VueloRobotat.orbit_marker(escala_velocidad=)`). Seguridad: supervisor de separacion a 30 cm, despegue rechazado a menos de 50 cm, y si uno cae el otro aterriza. 19 pruebas unitarias y 6 de integracion con el `VueloRobotat` simulado. Decision registrada en `30-Decisiones`. **Sin validar en vuelo.**
- **Claude**: analisis del primer vuelo en formacion (`60-Analisis/2026-09-18 Primer vuelo de dos drones en formacion por gestos.md`). Tres fallos: (1) orbitando llegaron a 0.29 m y aterrizo el supervisor -> repulsion directa entre drones y desfase mas fuerte en `formacion_camara.py`; (2) el marker va a 1.2-1.5 m y el techo fluido es 0.90: el error en Z se llevaba la velocidad (0.14 m/s en horizontal de 0.30) -> `altura_alcanzable` en `vuelo_camara.py`; (3) estaticos al reves por `--rumbo 0` con el operador mirando a ~155 grados: ADELANTE acerco los drones al operador. 12 pruebas nuevas, incluida una simulacion en lazo cerrado del fallo de la orbita. Sin validar en vuelo.
- **Claude**: segundo vuelo en formacion (17:51). Los seis estaticos funcionaron con el rumbo corregido. El Dron 2 cayo con motores cortados desde 0.87 m: su **telemetria se congelo 7.8 s** y la vigilancia comparo el mocap con un EKF viejo (0.098 -> 0.152 m). `dron_robotat.py`: `ekf_edad_s` y `TELEMETRIA_TIMEOUT_S`; con telemetria vieja no se evalua el error y se aterriza en vez de cortar. `CONFIRMACION_ABAJO_S = 0.45` porque bajar el brazo barria el cono de ABAJO. Nuevo `--grabar-video` (MP4 de la interfaz a velocidad real, `visualization/grabador.py`). 9 pruebas nuevas. Sin validar en vuelo.
- **Claude**: tercer vuelo en formacion. Acabo otra vez a 0.28 m en la orbita; causa aislada reproduciendo la logica sobre las posiciones reales: las **geocercas por dron** (despegues a 1.54 m, radio 1.0 -> zona comun de 0.46 m; cada cerca empujaba a su dron hacia el otro y ganaba a la repulsion) y una ley de orbita que no mantenia el radio (equilibrio en 0.37 m de 0.50). Geocerca comun y despegue rechazado si no caben, ley polar `velocidad_de_orbita`, repulsion desde 0.80 m, anclas a +-70 grados, radio minimo 0.60 m. 8 pruebas nuevas. Sin validar en vuelo.
- **Claude**: "hice el gesto y no vuelan" (sesiones 18:34 y 18:40). Era la comprobacion nueva de la geocerca comun rechazando el despegue, como esta disenada: drones a 1.60 m y 1.95 m entre si con `--radio-max 1.0` (hace falta 1.4 m o menos). El fallo era mio de comunicacion: el motivo solo salia por la consola. Ahora `VueloFormacion.aviso` lo da en una linea y `control_camara_dron1.py` la pinta en rojo en el panel, tambien cuando estan demasiado juntos. 3 pruebas nuevas.
- **Claude**: sesiones 18:46 y 18:50: el dron seguia sin senal y no habia forma de pararlo (59 s de SEGUIR con todos los aplausos rechazados). Causa de fondo: con la camara PTZ girando se descartaba el frame para los tres canales, **tambien el paro de emergencia**, que tolera 0.25 s de hueco. Cambios en `control_camara_dron1.py`: el paro recibe siempre la pose, la camara no gira durante la X, primera etapa del paro a 0.35 s que **frena** (hover) antes de aterrizar a 1 s, y antirrebote de 4 s entre comportamientos continuos. Zona de exclusion del marker (`vuelo_camara.fuera_del_marker`, `--exclusion-marker 0.60`) en todas las ordenes; `--radio-seguir 0.80` (era 0.45, `CameraMarkerFollower(follow_radius_m=)`) y `--radio-orbita 0.80`. 11 pruebas nuevas. Sin validar en vuelo.
- **Claude**: el gesto `arco` hace la pirueta de la demo de Bitcraze en IROS 2018 (espiral hacia abajo y subida por el eje), a peticion del operador. `arco` era ALEJARSE, sin implementacion de vuelo (pendiente en T-005): **cambia el vocabulario**, `MODOS_DINAMICOS['arco'] = 'PIRUETA'` en `recognition/vocabulario.py`. `vuelo_camara.py`: `PlanEspiral`, `punto_espiral` (radio y altura suavizados con senos, como Bitcraze), `VueloRobotat.pirueta()`; se vuela con velocidades en modo fluido, asi que pasa por geocerca, exclusion del marker y repulsion. `formacion_camara.py`: doble helice en oposicion alrededor del punto medio con avance comun. 13 pruebas nuevas, con vuelos simulados completos de uno y de dos drones (separacion minima > 0.80 m). Sin validar en vuelo.
- **Claude**: dos consultas sin cambio de codigo. (1) Si el operador puede **definir sus propios gestos**: los dinamicos ya se definen con ejemplos (`grabar_gestos.py --gesto`, `construir_plantillas.py --gestos`), pero que hace cada gesto esta fijo en `recognition/vocabulario.py` (`MODOS_DINAMICOS`, `CONMUTADOR`, `SENALERO`) y un nombre nuevo se reconoce y se ignora; los estaticos son reglas geometricas, no ejemplos. Propuesto y no hecho: un archivo de configuracion gesto -> accion. (2) Si el Crazyflie 2.1 puede hacer **acrobacias**: el hardware da para volteretas (empuje-peso ~2:1, hay videos de la comunidad), pero el firmware corta motores al inclinarse (deteccion de vuelco), el nucleo solo manda posiciones y velocidades, y boca abajo Motive perderia el rigid body. Alternativa elegida: trayectorias vistosas con el mismo control, que acabo siendo la pirueta del gesto `arco`.
- **Claude**: incidente de proceso, sin perdida. Al final de un comando de pruebas se me colo un `git stash` que guardo todos los cambios locales del operador; detectado en el momento y restaurado con `git stash pop` (15 archivos, ningun stash pendiente). Lo unico que pudo cambiar: algo que estuviera añadido con `git add` aparece como modificado sin añadir.
- **Codex**:
- **Gemini**:

## Bloqueos y dudas
- **Telemetría que se congela con dos drones**: 8-39 s en cuatro sesiones de la tarde; con un dron, nunca más de 0.3 s en vuelo. Causa sin determinar (¿enlace de bajada con dos Crazyradio?). Ya no corta motores, pero aterriza el vuelo.
- **La cámara da la vuelta ~5 s al cruzar el centro**, sin gestos durante ese rato: el tope mecánico del pan cae justo hacia el área de vuelo y la base no se puede girar. El paro ya ve la imagen con la cámara girando, pero durante la vuelta entera no hay imagen útil. En vuelo, la emergencia de esos segundos es ESC.
- **El aplauso falla si el operador camina o la cámara se mueve** ("demasiado largo", "pose perdida"). Para parar se usa la X (frena a 0.35 s); para cambiar de modo hay que aplaudir parado.
- **`ven_aca` fantasma**: un caso aislado (18:47) por la cola de un `circulo`, tapado con un antirrebote de 4 s. No hay medida de falsos disparos en reposo (paso 2 de T-005, pendiente).
- IZQUIERDA es el estático más flojo (66 % por frame; en vuelo sale a trozos): el brazo izquierdo se ve más adelantado que el derecho y no se sabe por qué.
- Baterías: los vuelos de la tarde despegaron con 3.24-3.36 V bajo carga y bajaron a 2.96-3.10 V.
- Resuelto durante el día, por si se lee esta nota más tarde: la cámara "descolocada" (era el tope del pan más la convención de `PositionABS`), el encoder sin aplicar (aplicado: audio fuera, sub-stream a 1024 kbps; 704x480 no lo admite) y todo lo relativo al montaje de lado, que ya no se usa.

## Mañana
- **Validar en vuelo lo que sólo está probado en simulación**, de menos a más riesgo y con un dron antes que con dos: la X que frena, la zona de exclusión del marker (0.60 m) y los radios nuevos de seguir y orbitar (0.80 m), la pirueta del `arco`, y con dos drones la geocerca común, la repulsión (desde 0.80 m) y la órbita que mantiene el radio.
- Comprobar que una telemetría congelada aterriza en vez de cortar (no se puede provocar: mirar el CSV si vuelve a pasar) y buscar la causa con dos radios.
- Medir la latencia real de la cámara con `mapeo3d/apps/diagnose_camera.py --sin-pose`; sigue siendo una estimación.
- T-005: decidir qué pasa con el criterio de ALEJARSE, porque `arco` ahora es la pirueta; y hacer la sesión de falsos disparos en reposo.
- Mejoras pequeñas anotadas: que `probar_gestos_3d.py --practica` guarde en el CSV qué gesto está pidiendo; que el CSV del controlador guarde las medidas del brazo (no se pudo saber por qué DERECHA falló a las 17:33); un archivo de configuración gesto → acción.
- La tabla de ejes para la cámara montada de lado (`Rotate90`) nunca se validó y puede que el firmware también la compense: repetir la prueba de pulsos si se vuelve a ese montaje.
